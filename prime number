def isprime(n):
    if n<2:
        return False
    for i in range(2,n):
        if n%i==0:
            return False
        return True
def checksum(n):
    for i in range(2,n):
        if isprime(i) and isprime(num-i):
            print(num,"=",i,"+",num-i)
            return
    print("Not possible")
num=int(input("Enter a number:"))
checksum(num)
